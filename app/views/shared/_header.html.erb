<header class="bg-secondary text-secondary-content shadow-md">
  <div class="container mx-auto p-4 flex justify-between items-center">
    <div class="text-2xl font-bold tracking-tight">
      <%= link_to "PosiRem!", root_path, class: "btn btn-ghost btn-secondary text-secondary-content text-2xl md:text-3xl" %>
    </div>
    <nav>
      <ul class="flex items-center md:space-x-4">
        <li>
          <%= link_to "Home", root_path, class: "btn btn-ghost btn-secondary btn-sm md:btn-md" %>
        </li>
        <li>
          <%# ログインボタン %>
          <%= link_to `#`, class: "btn btn-ghost btn-secondary btn-sm md:btn-md flex gap-1" do %>
            <span><%# t('header.login') %>Login</span>
            <span class="material-symbols-rounded ms-size-24 md:ms-size-32">login</span>
          <% end %>
        </li>
      </ul>
    </nav>
  </div>
</header>
